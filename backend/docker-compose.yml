version: "3.9"

services:
  backend:
    build: .
    container_name: dail_backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DATABASE_URL: ${DATABASE_URL:-mysql+pymysql://app:app@127.0.0.1:3306/auth_service}
    restart: unless-stopped
